// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math/big"
	"server/ent/transaction"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ethereum/go-ethereum/common"
)

// TransactionCreate is the builder for creating a Transaction entity.
type TransactionCreate struct {
	config
	mutation *TransactionMutation
	hooks    []Hook
}

// SetBlockNumber sets the "blockNumber" field.
func (tc *TransactionCreate) SetBlockNumber(u uint64) *TransactionCreate {
	tc.mutation.SetBlockNumber(u)
	return tc
}

// SetTxHash sets the "txHash" field.
func (tc *TransactionCreate) SetTxHash(c common.Hash) *TransactionCreate {
	tc.mutation.SetTxHash(c)
	return tc
}

// SetFrom sets the "from" field.
func (tc *TransactionCreate) SetFrom(c common.Address) *TransactionCreate {
	tc.mutation.SetFrom(c)
	return tc
}

// SetTo sets the "to" field.
func (tc *TransactionCreate) SetTo(c common.Address) *TransactionCreate {
	tc.mutation.SetTo(c)
	return tc
}

// SetTokenId sets the "tokenId" field.
func (tc *TransactionCreate) SetTokenId(b *big.Int) *TransactionCreate {
	tc.mutation.SetTokenId(b)
	return tc
}

// Mutation returns the TransactionMutation object of the builder.
func (tc *TransactionCreate) Mutation() *TransactionMutation {
	return tc.mutation
}

// Save creates the Transaction in the database.
func (tc *TransactionCreate) Save(ctx context.Context) (*Transaction, error) {
	return withHooks(ctx, tc.sqlSave, tc.mutation, tc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tc *TransactionCreate) SaveX(ctx context.Context) *Transaction {
	v, err := tc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tc *TransactionCreate) Exec(ctx context.Context) error {
	_, err := tc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tc *TransactionCreate) ExecX(ctx context.Context) {
	if err := tc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tc *TransactionCreate) check() error {
	if _, ok := tc.mutation.BlockNumber(); !ok {
		return &ValidationError{Name: "blockNumber", err: errors.New(`ent: missing required field "Transaction.blockNumber"`)}
	}
	if _, ok := tc.mutation.TxHash(); !ok {
		return &ValidationError{Name: "txHash", err: errors.New(`ent: missing required field "Transaction.txHash"`)}
	}
	if v, ok := tc.mutation.TxHash(); ok {
		if err := transaction.TxHashValidator(v.String()); err != nil {
			return &ValidationError{Name: "txHash", err: fmt.Errorf(`ent: validator failed for field "Transaction.txHash": %w`, err)}
		}
	}
	if _, ok := tc.mutation.From(); !ok {
		return &ValidationError{Name: "from", err: errors.New(`ent: missing required field "Transaction.from"`)}
	}
	if v, ok := tc.mutation.From(); ok {
		if err := transaction.FromValidator(v.String()); err != nil {
			return &ValidationError{Name: "from", err: fmt.Errorf(`ent: validator failed for field "Transaction.from": %w`, err)}
		}
	}
	if _, ok := tc.mutation.To(); !ok {
		return &ValidationError{Name: "to", err: errors.New(`ent: missing required field "Transaction.to"`)}
	}
	if v, ok := tc.mutation.To(); ok {
		if err := transaction.ToValidator(v.String()); err != nil {
			return &ValidationError{Name: "to", err: fmt.Errorf(`ent: validator failed for field "Transaction.to": %w`, err)}
		}
	}
	if _, ok := tc.mutation.TokenId(); !ok {
		return &ValidationError{Name: "tokenId", err: errors.New(`ent: missing required field "Transaction.tokenId"`)}
	}
	return nil
}

func (tc *TransactionCreate) sqlSave(ctx context.Context) (*Transaction, error) {
	if err := tc.check(); err != nil {
		return nil, err
	}
	_node, _spec, err := tc.createSpec()
	if err != nil {
		return nil, err
	}
	if err := sqlgraph.CreateNode(ctx, tc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	tc.mutation.id = &_node.ID
	tc.mutation.done = true
	return _node, nil
}

func (tc *TransactionCreate) createSpec() (*Transaction, *sqlgraph.CreateSpec, error) {
	var (
		_node = &Transaction{config: tc.config}
		_spec = sqlgraph.NewCreateSpec(transaction.Table, sqlgraph.NewFieldSpec(transaction.FieldID, field.TypeInt))
	)
	if value, ok := tc.mutation.BlockNumber(); ok {
		_spec.SetField(transaction.FieldBlockNumber, field.TypeUint64, value)
		_node.BlockNumber = value
	}
	if value, ok := tc.mutation.TxHash(); ok {
		vv, err := transaction.ValueScanner.TxHash.Value(value)
		if err != nil {
			return nil, nil, err
		}
		_spec.SetField(transaction.FieldTxHash, field.TypeString, vv)
		_node.TxHash = value
	}
	if value, ok := tc.mutation.From(); ok {
		vv, err := transaction.ValueScanner.From.Value(value)
		if err != nil {
			return nil, nil, err
		}
		_spec.SetField(transaction.FieldFrom, field.TypeString, vv)
		_node.From = value
	}
	if value, ok := tc.mutation.To(); ok {
		vv, err := transaction.ValueScanner.To.Value(value)
		if err != nil {
			return nil, nil, err
		}
		_spec.SetField(transaction.FieldTo, field.TypeString, vv)
		_node.To = value
	}
	if value, ok := tc.mutation.TokenId(); ok {
		vv, err := transaction.ValueScanner.TokenId.Value(value)
		if err != nil {
			return nil, nil, err
		}
		_spec.SetField(transaction.FieldTokenId, field.TypeString, vv)
		_node.TokenId = value
	}
	return _node, _spec, nil
}

// TransactionCreateBulk is the builder for creating many Transaction entities in bulk.
type TransactionCreateBulk struct {
	config
	err      error
	builders []*TransactionCreate
}

// Save creates the Transaction entities in the database.
func (tcb *TransactionCreateBulk) Save(ctx context.Context) ([]*Transaction, error) {
	if tcb.err != nil {
		return nil, tcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tcb.builders))
	nodes := make([]*Transaction, len(tcb.builders))
	mutators := make([]Mutator, len(tcb.builders))
	for i := range tcb.builders {
		func(i int, root context.Context) {
			builder := tcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TransactionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i], err = builder.createSpec()
				if err != nil {
					return nil, err
				}
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tcb *TransactionCreateBulk) SaveX(ctx context.Context) []*Transaction {
	v, err := tcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tcb *TransactionCreateBulk) Exec(ctx context.Context) error {
	_, err := tcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tcb *TransactionCreateBulk) ExecX(ctx context.Context) {
	if err := tcb.Exec(ctx); err != nil {
		panic(err)
	}
}
